// Diameter of the floating button, and how far it sits from the screen edge / safe area.
export const BUTTON_SIZE = 40;
export const BUTTON_SIZE_TV = 30;
export const BUTTON_MARGIN = 16;

// Below this drag distance a release counts as a tap rather than a corner change.
export const DRAG_TAP_THRESHOLD = 16;

// How far down the grid has to scroll before the button appears.
export const SCROLL_SHOW_THRESHOLD = 400;